{
  "lock": false,
  "tasks": {
    "upgrade": "deno run --no-lock -A https://deno.land/x/udd/main.ts $(find . -name '*.ts')",
    "check": "deno check --unstable $(find . -name '*.ts')",
    "lint": "deno lint --unstable $(find . -name '*.ts')",
    "format": "deno fmt --unstable $(find . -name '*.ts')",
  }
}
